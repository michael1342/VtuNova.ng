/**
 * Feature Flags Configuration for VTUNova
 * 
 * Toggle `REFERRALS_ENABLED` to switch the Referrals page and related actions on or off:
 * - `false`: Referrals page displays the "COMING SOON" overlay, locks controls (inert), and guards API requests.
 * - `true`: Referrals page is fully active with all controls, links, and transactions enabled.
 */
export const REFERRALS_ENABLED = false;
